
const base = 'https://workshift-bot.site';

const config = {
    apiBasePath: base,
    apiEventPath: `${base}/event`,
    apiOfferPath: `${base}/offer`,
    apiAuthPath: `${base}/auth`,
    apiUserPath: `${base}/user`,
    apiAnswerPath: `${base}/offer/answer`,
    apiFeedbackPath: `${base}/feedback`
};

export default config;
